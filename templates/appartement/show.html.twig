{% extends 'base.html.twig' %}

{% block title %}Appartement{% endblock %}

{% block body %}
    <h1>Appartement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appartement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ appartement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adresse_show', {'id': appartement.adresse.id}, {'appartement': appartement})}}">adresse</a>

    {{ include('appartement/_delete_form.html.twig') }}
{% endblock %}
