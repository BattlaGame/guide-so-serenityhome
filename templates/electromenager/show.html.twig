{% extends 'base.html.twig' %}

{% block title %}Electromenager{% endblock %}

{% block body %}
    <h1>Electromenager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ electromenager.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ electromenager.nom }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ electromenager.lien }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ electromenager.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_electromenager_index') }}">back to list</a>

    <a href="{{ path('app_electromenager_edit', {'id': electromenager.id}) }}">edit</a>

    {{ include('electromenager/_delete_form.html.twig') }}
{% endblock %}
