{% extends 'base.html.twig' %}

{% block title %}Wifi index{% endblock %}

{% block body %}
    <h1>Wifi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Mdp</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for wifi in wifis %}
            <tr>
                <td>{{ wifi.id }}</td>
                <td>{{ wifi.nom }}</td>
                <td>{{ wifi.mdp }}</td>
                <td>
                    <a href="{{ path('app_wifi_show', {'id': wifi.id}) }}">show</a>
                    <a href="{{ path('app_wifi_edit', {'id': wifi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_wifi_new') }}">Create new</a>
{% endblock %}
