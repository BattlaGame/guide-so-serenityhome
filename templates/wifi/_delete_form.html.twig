<form method="post" action="{{ path('app_wifi_delete', {'id': wifi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wifi.id) }}">
    <button class="btn">Delete</button>
</form>
