<form method="post" action="{{ path('app_checkin_delete', {'id': checkin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checkin.id) }}">
    <button class="btn">Delete</button>
</form>
